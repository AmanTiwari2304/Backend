<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsapp</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h3>All Chats</h3>
    <form action="/chats/new" method="get">
        <button>New Chat</button>
    </form>
    <% for( let chat of chats ) { %>
        <div class = "chat">
            <p>From : <%= chat.from %></p>
            <div class = "msg">
                <p><%= chat.msg %></p>
            </div>
            <p>Received by <%= chat.to %></p>
            <hr>
            <p>
                <%= chat.created_at ? chat.created_at.toString().split(" ")[4] : "No data available" %>
            </p>
            <p>
                <%= chat.created_at ? chat.created_at.toString().split(" ").slice(0,4).join("-") : "No data available" %>
            </p>
            <form action="/chats/<%=chat._id%>/edit" method="get">
                <button>Edit</button>
            </form>

            <form action="/chats/<%=chat._id%>?_method=DELETE" method="POST">
                <button>Delete</button>
            </form>
              
            <form action="/chats/<%=chat._id%>/edit" method="get">
                <button>Show</button>
            </form>
        </div>
    <% } %>
</body>
</html>

